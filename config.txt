# file documenting which packages where installed during the development of pepDB
gem install haml
gem install sinatra
gem install sass
zypper in ruby19-devel sqlite3 sqlite3-devel rubygem-sqlite3
gem install sequel
gem install shotgun
gem install sinatra-partial
zypper in apache2
gem install passenger
passenger-install-apache2-module1.9
zypper in gcc-c++
zypper in libcurl-devel
zypper in libopenssl-devel
zypper in apache2-devel
vi /etc/apache2/httpd.conf.pepdb
gem install rack-test
gem1.9 install json
zypper in pcre-devel
make sqlite-regexp-master
gem install sinatra-static-assets -s http://gemcutter.org
gem install local sinatra-authentication
gem install rack-flash3
