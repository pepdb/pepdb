%fieldset.checklist
  - if !params['checkedElem'].nil?
    - @data_to_display.each do |elem|
      .radio
        %label
          = elem[@column]
          %input{:type => "radio", :name => params['radioName'],:id => "c_" << "#{elem[@column]}",:value => "#{elem[@column]}"}/
