%p
  %label
    = labelname 
  %select{:name => para, :id => para }
    %option
      &nbsp;
    - values.all.each do |elem|
      - if elem[column] == params[para]
        %option{:selected => true}
          = elem[column]
      - else
        %option                    
          = elem[column]

