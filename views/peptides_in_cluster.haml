%table{:border=> 1}
  %thead
    %tr
      - data_to_display.first.each do |column_name|
        %th= column_name[0].to_s.tr('_', ' ')
  %tbody
    - data_to_display.each do |data_element|
      %tr
        - data_element.each do |cell_data|
          %td= cell_data[1]

